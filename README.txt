# Ассистент Печати (@Seals18bot)

Этот Telegram-бот принимает заказы от клиентов, отправляет их в ИИ (Gemini) и сохраняет результат в Excel-файл `orders.xlsx`, который можно открыть в LibreOffice или Excel.

## 🚀 Как запустить

1. Установи Node.js
2. Установи зависимости:

    npm install

3. Запусти бота:

    node bot.js

## 🔐 Ключи

- Telegram Token: указан в `config.js`
- Google Gemini API: указан в `config.js`

## 📄 Где сохраняются заказы?`

В файл `orders.xlsx` в корне проекта. Каждый заказ добавляется как новая строка.

## 🔁 Автоотправка файла сотруднику

После каждого нового заказа бот отправляет `orders.xlsx` в Telegram-профиль: **@eles007**.

---